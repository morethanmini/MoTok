/**
 * 화면 기능 스위치 — 배포 없이 코드 한 줄로 끄고 켜는 것만 둔다.
 */

/**
 * 포인트 충전(결제) 진입점.
 *
 * <p><b>false = 유저 테스트 기간 동안 잠금.</b> 충전 UI를 숨기고 클릭도 막는다.</p>
 *
 * <p>끈 이유 — PG 연동이 아직 TODO라({@code ChargePointsModal}의 confirm은 `charged`만 emit),
 * 지금 누르면 <b>서버 잔액은 그대로인데 화면 잔액만 올라간다.</b> 돈이 빠져나가지는 않지만
 * 테스터가 "충전했는데 구매가 안 된다"를 버그로 보고하게 되고, 게임 보상 포인트가 제대로
 * 도는지 보려는 테스트 데이터가 가짜 잔액에 섞인다.</p>
 *
 * <p>다시 열 때 — 이 값을 true로 바꾸기 전에 PG 연동과 서버 잔액 반영이 끝났는지 확인한다.
 * 진입점은 세 곳이다: 헤더 코인 버튼, 상점의 구매 확인 모달(포인트 부족 시), 그 모달의 안내 문구.</p>
 */
export const POINT_CHARGE_ENABLED = false
