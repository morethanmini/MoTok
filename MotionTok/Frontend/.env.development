# 개발 서버(5173)가 /api·/ws 를 localhost:8080 으로 프록시한다(vite.config.ts) — 상대 경로를 쓰면
# 브라우저 입장에서 프론트와 백엔드가 같은 오리진이 되어, 배포 빌드(VITE_API_BASE_URL=/api)와도 구성이 같아진다.
# 프록시를 거치지 않고 다른 백엔드를 보려면 .env.development.local 에 절대 URL로 덮어쓴다(git 무시 대상).
VITE_API_BASE_URL=/api
